/**
 * The code the welcome pop-up promises. One constant, read by the pop-up
 * (to display it) and by the /newsletter route (to record which offer a
 * sign-up is owed) — so the two can never drift apart.
 */
export const NEWSLETTER_PROMO_CODE = 'REDA10';
